# yaml-language-server: $schema=https://toit.io/schemas/artemis/pod-specification/v1.json

$schema: https://toit.io/schemas/artemis/pod-specification/v1.json
sdk-version: "v2.0.0-alpha.142"
artemis-version: "v0.17.0"
chip: "esp32s3"
max-offline: "15m"
connections:
  - type: "wifi"
    ssid: "InternetToDaMax"
    password: "ichschnorremirwlanbeimirko"
containers:
  trigger:
    entrypoint: "../espnow/trigger.toit"
    triggers:
      - gpio:
          - pin: 2
            level: "low"